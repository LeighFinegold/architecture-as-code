apiVersion: v1
kind: Service
metadata:
  name: {{name}}-svc
spec:
  selector:
    app: {{name}}
  type: LoadBalancer
  ports:
    {{#each ports}}
    - protocol: TCP
      port: {{this}}
      targetPort: {{this}}
    {{/each}}
